[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests that load model weights or are slow (deselect with '-m "not slow"')
    integration: e2e tests that exercise real models or the filesystem heavily
